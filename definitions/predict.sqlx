config { type: 'table', dependencies: ['model_evaluate']}

SELECT
  *
FROM
  ML.PREDICT (MODEL ${ref('bq_model')},
    (
    SELECT
      *
    FROM
      ${ref('census_input')}
    WHERE
      dataframe = 'prediction'
     )
  )